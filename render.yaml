services:
  - type: web
    name: ems_project
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn ems_project.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 2"
    autoDeploy: true
